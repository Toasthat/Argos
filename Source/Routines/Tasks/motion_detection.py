"""This is currently not going to be
used because average change in pixel values,
i.e., what motion detection basically is,
can interpret shadows as movement. That is
a no go in terms of a security system, as
calling the cops on a shadow is a big no-no."""

#import numpy as np
import cv2

if __name__ == "__main__":
    cv2.VideoCapture()
